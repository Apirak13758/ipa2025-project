# TextFSM template for Cisco IOS 'show ip route'
# Variables: NETWORK, MASK, NEXT_HOP, INTERFACE, etc.
Value NETWORK (\S+)
Value MASK (\S+)
Value NEXT_HOP (\S+)
Value INTERFACE (\S+)

Start
  ^O\s+${NETWORK}/${MASK}\s+\[.*\]\s+via\s+${NEXT_HOP},\s+${INTERFACE}
  ^C\s+${NETWORK}/${MASK}\s+is directly connected,\s+${INTERFACE}
  # ...add more patterns as needed
